# Data Collection

Active collection and replay utilities live here.

- `main.py`: local entry script for the collection workflow
- `experiment.py`: experiment runtime
- `recorder.py`: LSL recorder that now writes into `data/NIC2/`
- `eeg_replay_plotter.py`: CSV replay and visualization utility
- `rename_channels.py`: channel renaming utility
