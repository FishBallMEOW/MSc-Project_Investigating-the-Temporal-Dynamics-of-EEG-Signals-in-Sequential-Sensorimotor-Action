# Handoff: data-and-code-reorg-plan

- Date: 2026-03-13
- Author: Codex
- Summary: Committed the path/docs cleanup changes and produced the implementation-ready plan for organizing the local data area and the remaining repository structure.
- Completed:
  - committed source/docs/path cleanup as `0222e4b`
  - excluded local `data/`, cache artifacts, and untracked large results from that commit
  - defined the target cleanup phases for `data/`, `OOP_ver/`, and the root source layout
- Files changed:
  - `docs/plans/2026-03-13-data-and-code-reorg-plan.md`
  - `docs/tasks/active/data-and-code-reorg-plan.md`
  - `docs/handoffs/latest.md`
  - `docs/handoffs/2026-03-13-data-and-code-reorg-plan.md`
  - `docs/status/current.md`
- Decisions made:
  - `data/NIC2/` is the only active dataset folder
  - `data/NIC2 - Copy/` is archive-only, not active
  - all non-`NIC2` local datasets should move into `data/_archive/`
  - the next implementation pass should reorganize code into active, archival, and generated-output areas explicitly
- Validation performed:
  - confirmed the repo is ahead by one commit after `0222e4b`
  - confirmed local data remains uncommitted
  - confirmed the reorganization plan is decision-complete enough for a later implementation pass
- Open issues / risks:
  - active canonical scripts and notebooks still need selection during implementation
  - local archive data is still physically mixed under `data/` until the next pass runs
  - untracked large result files remain present locally
- Exact next steps:
  - implement the `data/` move so only `data/NIC2/` remains active
  - update `.gitignore` to allow `data/NIC2/` while keeping `data/_archive/` ignored
  - classify and move active versus archive source files out of the root and `OOP_ver/`
- Suggested owner type: cleanup-focused implementer
