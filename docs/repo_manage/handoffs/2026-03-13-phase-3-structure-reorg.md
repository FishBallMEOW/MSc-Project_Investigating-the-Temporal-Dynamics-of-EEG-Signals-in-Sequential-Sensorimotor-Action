# Handoff: phase-3-structure-reorg

- Date: 2026-03-13
- Author: Codex
- Summary: Executed the source-layout reorganization that dissolved `OOP_ver/` and redistributed active code, notebooks, archives, and generated artifacts into named top-level folders.
- Completed:
  - moved active pipelines into `src/pipelines/`
  - moved active collection utilities into `src/data_collection/`
  - renamed the retained main notebook to `notebooks/main/eeg_analysis_main.ipynb`
  - moved historical notebooks, root prototypes, MATLAB files, reports, and pipeline backups into archive areas
  - moved generated outputs into `artifacts/`
  - updated active code defaults to use repo-rooted `data/`, `archive/`, and `artifacts/` paths
  - updated current-facing docs to describe the new layout
- Files changed:
  - active source under `src/`
  - working and archived notebooks under `notebooks/`
  - archived source under `archive/`
  - generated outputs under `artifacts/`
  - current docs and README
- Decisions made:
  - `src/pipelines/eeg_processing_organized.py` remains the current canonical pipeline entrypoint
  - `src/data_collection/` remains a separate grouped runtime area
  - `notebooks/main/eeg_analysis_main.ipynb` is the single active notebook
  - generated outputs belong under `artifacts/` rather than source folders
- Validation performed:
  - verified the new top-level structure exists
  - updated path assumptions in active source files that changed under the move
  - updated current-facing docs to remove `OOP_ver/` as the active workspace
- Open issues / risks:
  - support pipeline variants in `src/pipelines/` still need a later keep-versus-archive decision
  - large local result files in `artifacts/results/` still need a final retention policy
  - cache artifacts need to remain ignored and out of future commits
- Exact next steps:
  - finalize Git tracking so caches and generated artifacts are not staged
  - run targeted smoke checks on the moved pipeline and data-collection entrypoints
  - decide whether `eeg_processing.py`, `process.py`, and `processing.py` stay active or move to archive
- Suggested owner type:
  - implementer / reviewer

